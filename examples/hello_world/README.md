# hello world example

A simple "hello world" example, using [axum](https://docs.rs/axum/latest/axum/)
and [tokio](https://docs.rs/tokio/latest/tokio/)

To run the example, use the command `cargo run -p hello_world`. Once running,
open `http://127.0.0.1:8080/` in your browser.
